#include "main.h"

/**
 * print_alphabet - prints alpha in lowercase
 * Return: Always 0
 */

void print_alphabet(void)
{
	char ch;

	for (ch = 'a'; la <= 'z'; za++)
	{
		_putchar(ch);
	_putchar('\n');
	}
}
